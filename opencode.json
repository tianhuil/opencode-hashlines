{
  "$schema": "https://opencode.ai/config.json",
  "description": "Hashlines plugin for OpenCode - provides hash-anchored file reading and editing to prevent corruption from stale line references",
  "homepage": "https://github.com/tianhuil/opencode-hashlines",
  "author": "tianhuil",
  "license": "MIT",
  "keywords": ["hashlines", "file-editing", "hash-anchored", "line-editing"],
  "agent": {
    "build": {
      "tools": { "edit": false },
      "prompt": "Always use `hashread` to read files and `hashedit` to edit them. Never use `edit` or `str_replace`. The hashread output format is `N:hh|line` — pass those hashes back to hashedit as anchors."
    }
  }
}
